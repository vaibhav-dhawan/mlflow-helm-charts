#helm upgrade mlflow --namespace mlflow ./mlflow --set persistence.storageClass=managed-premium
# helm template mlflow --namespace mlflow ./mlflow -f mlflowt8495.yaml > template.yaml
defaultArtifactRoot: s3://mlflow-domino-artifacts-946429944765

aws:
  enabled: True
  defaultRegion: us-west-2
  accessKeyID: test
  secretAccessKey: test

# persistence:
#   storageClass: managed-premium